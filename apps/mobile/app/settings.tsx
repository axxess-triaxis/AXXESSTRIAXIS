import { Screen } from "../src/components/screen";

export default function Settings() {
  return <Screen title="Settings" subtitle="Profile, workspace, security, analytics, and beta preferences." />;
}
