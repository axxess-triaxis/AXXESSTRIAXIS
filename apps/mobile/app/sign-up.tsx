import { Screen } from "../src/components/screen";

export default function SignUp() {
  return <Screen title="Sign up" subtitle="Create the account on web first, verify email, then continue mobile onboarding." />;
}
