import { Screen } from "../src/components/screen";

export default function Onboarding() {
  return <Screen title="Onboarding" subtitle="Create or join an organization, select sector, create workspace, and accept beta notices." />;
}
