import { Screen } from "../src/components/screen";

export default function Notifications() {
  return <Screen title="Notifications" subtitle="Tenant-scoped alerts, governance notices, and SLA warnings." />;
}
