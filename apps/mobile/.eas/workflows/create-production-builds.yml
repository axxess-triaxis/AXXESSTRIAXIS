name: Create Production Builds

on:
  workflow_dispatch: {}

jobs:
  build_android:
    type: build
    environment: production
    params:
      platform: android
      profile: production

  build_ios:
    type: build
    environment: production
    params:
      platform: ios
      profile: production

