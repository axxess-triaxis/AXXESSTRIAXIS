import { Users } from "lucide-react";
import { LitePlaceholderSection } from "../../../features/lite/sections/LitePlaceholderSection";

export default function LitePeoplePage() {
  return <LitePlaceholderSection title="People" description="Your customers, vendors, and contacts will show up here." icon={Users} />;
}
