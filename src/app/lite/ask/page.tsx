import { MessagesSquare } from "lucide-react";
import { LitePlaceholderSection } from "../../../features/lite/sections/LitePlaceholderSection";

export default function LiteAskPage() {
  return <LitePlaceholderSection title="Ask AXXESS" description="Ask a question about your own uploaded files and get a cited answer." icon={MessagesSquare} />;
}
