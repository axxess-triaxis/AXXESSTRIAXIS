import { createDemoDataset } from "../../demo/demoDataset";

const dataset = createDemoDataset();

export const fallbackDocumentCategories = dataset.documentCategories;
export const fallbackDocumentTags = dataset.documentTags;
export const fallbackDocuments = dataset.documents;
export const fallbackKnowledgeArticles = dataset.knowledgeArticles;
export const fallbackDocumentActivity = dataset.documentActivity;
